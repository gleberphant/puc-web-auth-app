info:
  name: exibir USUARIO
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:4000/usuario/1
  body:
    type: json
    data: |-
      {
        "uid": "2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
