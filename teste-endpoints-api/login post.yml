info:
  name: login post
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:4000/login
  body:
    type: json
    data: |-
      {
        "login": "admin@admin",
        "senha": "admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
